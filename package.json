{
  "name": "koa-redux",
  "devDependencies": {
    "@types/koa": "^2.11.7",
    "@types/koa-router": "^7.4.1",
    "typescript": "^4.1.3",
    "typescript-cli": "^0.1.0"
  },
  "dependencies": {
    "koa": "^2.13.1",
    "koa-router": "^10.0.0"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "server": "node dist/server"
  }
}
